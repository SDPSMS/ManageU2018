import { StyleSheet } from 'react-native'
import { Metrics, Colors } from '../../Themes'

export default {
  container: {
    margin: Metrics.smallMargin,
    alignItems: 'center',
    alignSelf: 'center',
    color: Colors.error
  }
}
